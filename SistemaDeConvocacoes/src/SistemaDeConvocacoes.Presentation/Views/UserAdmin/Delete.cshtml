@model SistemaDeConvocacoes.Domain.Models.ApplicationUser

@{
    Layout = "~/Views/Shared/Admin/_LayoutAdmin.cshtml";
    ViewBag.Title = "Deletar Usuário.";
}


<div class="col-lg-12">    
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">@ViewBag.Title</h6>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="panel-body">
                    <div class="card-body">
                        <div class="table-responsive">
                            <div class="table-responsive">
                                
                                <h6><b>Você tem certeza que deseja excluir este usuário?</b></h6>
                                <div>
                                    <dl class="dl-horizontal">
                                        <dt>
                                            @*@Html.DisplayNameFor(model => model.UserName)*@Nome do usuário
                                        </dt>

                                        <dd>
                                            @Html.DisplayFor(model => model.UserName)
                                        </dd>
                                    </dl>

                                    @using (Html.BeginForm())
                                    {
                                        @Html.AntiForgeryToken()

                                        <div class="form-actions no-color">
                                            <input type="submit" value="Delete" class="btn btn-danger" />
                                            <button type="button" class="btn btn-secondary" title="" data-placement="left" onclick="history.back();">Voltar</button>

                                        </div>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
</div>
