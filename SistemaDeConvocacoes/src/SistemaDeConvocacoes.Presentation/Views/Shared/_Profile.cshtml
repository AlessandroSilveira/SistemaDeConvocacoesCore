@using Microsoft.AspNet.Identity
<ul class="nav navbar-top-links navbar-right">
    <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <i class="fa fa-user fa-fw"></i> <i class="fa fa-caret-down"></i>
        </a>
        <ul class="dropdown-menu dropdown-user" style="min-width:250px !important">
            <li>
                @if (Request.IsAuthenticated)
                {
                    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm"}))
                    {
                        @Html.AntiForgeryToken()
                        <a href="#"><i class="fa fa-user fa-fw"></i> @User.Identity.GetUserName()</a>
                    }
                }
                </li>
            <li class="divider"></li>
            @*<li>
                <a href="#"><i class="fa fa-user fa-fw"></i> Editar meu perfil</a>
            </li>*@
            <li class="divider"></li>
            <li>
                <a style="color: black !important" href="javascript:document.getElementById('logoutForm').submit()">Sair</a>
            </li>
        </ul>
    </li>
</ul>